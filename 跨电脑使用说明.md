# 小红书配图工具 · 跨电脑使用说明

## 一、核心概念：部署 ≠ 联网

- **部署** = 把 `index.html`、`version.js`、`update_log.js` 这几个文件放到某个"能被打开的位置"。
- 这个位置可以是：
  - 你自己电脑的文件夹（`file://` 双击打开）
  - 同一局域网内的共享位置 / 内网服务器
  - 公网服务器 / 静态托管平台（GitHub Pages、Vercel、阿里云 OSS 等）

只有"公网服务器"才需要联网，**局域网和本地部署都不需要外网**。

---

## 二、另一台电脑能不能用？

取决于你给对方的"网址"是什么类型：

| 你给对方的网址 | 另一台电脑能用吗 | 说明 |
|---|---|---|
| `file:///C:/xxx/index.html`（本地文件路径） | ❌ 不能 | 这是你电脑上的路径，别人电脑没有这个文件 |
| `http://192.168.x.x/index.html`（局域网 IP） | ✅ 同一 Wi-Fi/局域网内能 | 需两台电脑在同一局域网，且你的电脑在运行静态服务 |
| `https://xxx.pages.dev/index.html`（公网网址） | ✅ 任何能上网的电脑都能用 | 推荐方式 |

**结论**：如果想让另一台电脑用，必须把文件部署到公网或局域网服务器，给对方服务器上的网址，再给口令。

---

## 三、开发者模式的新模板能推送给用户吗？

✅ **可以**，流程如下：

### 推送流程

1. 在你的工具里进入开发者模式（输入用户口令 → 点版本号 5 次 → 输入管理口令）
2. 编辑好新模板 / 调整预设模板
3. 管理后台点 **「📦 导出全部预设模板 JSON」**，得到一个 JSON 文件
4. 打开 **口令工具.html**（发布台），连上你的 `index.html`
5. 上传刚导出的模板 JSON（可选：同时改口令、升版本号）
6. 点 **「🚀 一键发布」**，会生成新文件压缩包（升版本号时含 `index.html` + `version.js`/`version.json` + `_git_push.bat`）
7. 用压缩包里的文件覆盖本地仓库旧文件，双击 `_git_push.bat` 即自动推送到 GitHub Pages（仅换口令/模板且没升版本号时，不推送，随下次改版一起上线）
8. 买家端打开工具时，顶部会出现 **「工具已更新」提示条**，买家点「立即刷新」即可看到新模板

> 自定义模板保存在买家浏览器本地，不受推送影响；只有预设模板会被整库替换。

---

## 四、部署方式选择

### 方式 A：公网部署（推荐，任何电脑可用）

**方式 A1：GitHub Pages（免费，已部署 ✅）**

本工具已部署到 GitHub Pages，网址：

- 工具主页：`https://lijinbu666.github.io/xhs-image-tool/`
- 口令工具：`https://lijinbu666.github.io/xhs-image-tool/口令工具.html`

**以后更新模板/口令**（在你电脑上操作）：

1. 用发布台（口令工具.html）生成新文件（`index.html`，升版本号时还会生成 `version.js` / `version.json`）
2. 发布时勾选「生成 GitHub 一键推送脚本」，工具文件夹里会得到一个 `_git_push.bat`
3. **双击 `_git_push.bat`**，它会自动完成 `git add → commit → push`（要求电脑已装 Git、文件夹是从仓库 clone 的、本机 Git 登录过 GitHub，不需要 Token）
4. 等待约 1 分钟，GitHub Pages 自动重新构建，买家端刷新即可看到更新

> 不想双击脚本时也可手动打开 PowerShell 执行：
> ```powershell
> cd "C:\Users\28903\Documents\trae_projects\Easy to generate images"
> git add -A
> git commit -m "更新模板/口令"
> git push
> ```

> **仅换口令/模板、工具本身没改时**：在发布台取消勾选「同步升级版本号」，此时不会生成 version 文件、也不需要推送 GitHub——新内容保存在本地文件里，随下次工具改版一起上线即可。

### 方式 B：局域网部署（同一 Wi-Fi 内的电脑可用）

在你电脑上运行一个静态服务器（任选其一）：

- Python：`python -m http.server 8080`
- Node：`npx serve .`
- VS Code：安装 "Live Server" 插件

然后对方访问 `http://你的电脑IP:8080/index.html`。

> 缺点：你电脑关机或断开 Wi-Fi 后对方就用不了了。

### 方式 C：纯本地文件（只能本电脑用）

直接双击 `index.html` 打开。不能给别人用，但功能完整。

---

## 五、需要的手续文件清单

把工具给另一台电脑的人使用，需要准备：

### 给买家的
- ✅ 工具网址（公网或局域网地址）
- ✅ 用户口令（登录用）

### 给开发者（你自己保留）
- ✅ 管理口令（进入开发者模式）
- ✅ 原始 `index.html`、`口令工具.html`、`version.js`、`update_log.js`
- ✅ 预设模板 JSON 备份（管理后台可导出）

### 发布时自动生成
- 发布台一键生成的新 `index.html`（升版本号时还有 `version.js` / `version.json`）
- `_git_push.bat`：双击即可自动推送 GitHub Pages（升版本号发布时才生成）

---

## 六、口令修改流程

1. 打开 `口令工具.html`
2. 连上你的 `index.html`
3. 输入新的用户口令 / 管理口令
4. 改了工具本身就保留勾选「同步升级版本号」，点「一键发布」生成新文件并双击 `_git_push.bat` 推送
5. 通知买家新口令

> 仅换口令、工具没改版时：取消勾选「同步升级版本号」，只生成新 `index.html`，不升版本号、不推送 GitHub，随下次改版一并上线。
>
> 口令哈希写死在 `index.html` 里，所以改口令必须更新该文件，不能只在界面上改。

---

## 七、常见问题

**Q：买家没看到更新提示条？**
A：让买家刷新页面（Ctrl+F5 强制刷新）。version.json 需要比本地版本号高才会提示。

**Q：另一台电脑打开网址白屏？**
A：检查网址是否正确、服务器是否在运行、文件是否上传完整（index.html + version.js 都要有）。

**Q：推送模板后买家的自定义模板会丢吗？**
A：不会。自定义模板存在买家浏览器本地，推送只替换预设模板库。

**Q：手机能用吗？**
A：公网网址在手机浏览器也能打开，但工具主要面向电脑端操作。
